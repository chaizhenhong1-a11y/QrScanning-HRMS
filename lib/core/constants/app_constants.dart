abstract final class AppConstants {
  static const String appName = 'Veyra HRMS';
  static const Duration attendanceQrLifetime = Duration(seconds: 90);
  static const Duration allowedClockSkew = Duration(seconds: 10);
}
